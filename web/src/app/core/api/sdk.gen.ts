// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AddArtifactApiV1ReleasesIdArtifactsPostData, AddArtifactApiV1ReleasesIdArtifactsPostErrors, AddArtifactApiV1ReleasesIdArtifactsPostResponses, AddRuleApiV1ProfilesProfileIdRulesPostData, AddRuleApiV1ProfilesProfileIdRulesPostErrors, AddRuleApiV1ProfilesProfileIdRulesPostResponses, AdminActivateUserApiV1AdminUsersUserIdActivatePatchData, AdminActivateUserApiV1AdminUsersUserIdActivatePatchErrors, AdminActivateUserApiV1AdminUsersUserIdActivatePatchResponses, AdminCreateUserApiV1AdminUsersPostData, AdminCreateUserApiV1AdminUsersPostErrors, AdminCreateUserApiV1AdminUsersPostResponses, AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchData, AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchErrors, AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchResponses, AdminListUsersApiV1AdminUsersGetData, AdminListUsersApiV1AdminUsersGetErrors, AdminListUsersApiV1AdminUsersGetResponses, AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchData, AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchErrors, AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchResponses, ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostData, ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostErrors, ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostResponses, ArchiveReleaseApiV1ReleasesIdArchivePostData, ArchiveReleaseApiV1ReleasesIdArchivePostErrors, ArchiveReleaseApiV1ReleasesIdArchivePostResponses, ArchiveTemplateApiV1TemplatesTemplateIdArchivePostData, ArchiveTemplateApiV1TemplatesTemplateIdArchivePostErrors, ArchiveTemplateApiV1TemplatesTemplateIdArchivePostResponses, ChangePasswordApiV1UsersMePasswordPostData, ChangePasswordApiV1UsersMePasswordPostErrors, ChangePasswordApiV1UsersMePasswordPostResponses, CloneTemplateApiV1TemplatesTemplateIdClonePostData, CloneTemplateApiV1TemplatesTemplateIdClonePostErrors, CloneTemplateApiV1TemplatesTemplateIdClonePostResponses, ConfigureNotificationChannelApiV1NotificationsChannelsPostData, ConfigureNotificationChannelApiV1NotificationsChannelsPostErrors, ConfigureNotificationChannelApiV1NotificationsChannelsPostResponses, CreateApiKeyApiV1UsersUserIdApiKeysPostData, CreateApiKeyApiV1UsersUserIdApiKeysPostErrors, CreateApiKeyApiV1UsersUserIdApiKeysPostResponses, CreateCustomRoleApiV1OrganizationsOrgIdRolesPostData, CreateCustomRoleApiV1OrganizationsOrgIdRolesPostErrors, CreateCustomRoleApiV1OrganizationsOrgIdRolesPostResponses, CreateOrganizationApiV1OrganizationsPostData, CreateOrganizationApiV1OrganizationsPostErrors, CreateOrganizationApiV1OrganizationsPostResponses, CreateProfileApiV1OrganizationsOrgIdProfilesPostData, CreateProfileApiV1OrganizationsOrgIdProfilesPostErrors, CreateProfileApiV1OrganizationsOrgIdProfilesPostResponses, CreateProjectApiV1OrganizationsOrgIdProjectsPostData, CreateProjectApiV1OrganizationsOrgIdProjectsPostErrors, CreateProjectApiV1OrganizationsOrgIdProjectsPostResponses, CreateReleaseApiV1ProjectsProjectIdReleasesPostData, CreateReleaseApiV1ProjectsProjectIdReleasesPostErrors, CreateReleaseApiV1ProjectsProjectIdReleasesPostResponses, CreateTemplateApiV1TemplatesPostData, CreateTemplateApiV1TemplatesPostErrors, CreateTemplateApiV1TemplatesPostResponses, DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteData, DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteErrors, DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteResponses, DeleteCustomRoleApiV1RolesRoleIdDeleteData, DeleteCustomRoleApiV1RolesRoleIdDeleteErrors, DeleteCustomRoleApiV1RolesRoleIdDeleteResponses, DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteData, DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteErrors, DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteResponses, DeleteProfileApiV1ProfilesProfileIdDeleteData, DeleteProfileApiV1ProfilesProfileIdDeleteErrors, DeleteProfileApiV1ProfilesProfileIdDeleteResponses, DeleteReleaseApiV1ReleasesIdDeleteData, DeleteReleaseApiV1ReleasesIdDeleteErrors, DeleteReleaseApiV1ReleasesIdDeleteResponses, DeleteRuleApiV1RulesRuleIdDeleteData, DeleteRuleApiV1RulesRuleIdDeleteErrors, DeleteRuleApiV1RulesRuleIdDeleteResponses, DeleteUserAccountApiV1UsersMeAccountDeleteData, DeleteUserAccountApiV1UsersMeAccountDeleteErrors, DeleteUserAccountApiV1UsersMeAccountDeleteResponses, ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetData, ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetErrors, ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetResponses, ExportUserDataApiV1UsersMeExportGetData, ExportUserDataApiV1UsersMeExportGetResponses, ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetData, ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetErrors, ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetResponses, GetCurrentUserProfileApiV1UsersMeGetData, GetCurrentUserProfileApiV1UsersMeGetResponses, GetDashboardMetricsApiV1DashboardMetricsGetData, GetDashboardMetricsApiV1DashboardMetricsGetErrors, GetDashboardMetricsApiV1DashboardMetricsGetResponses, GetNotificationPreferencesApiV1NotificationsPreferencesGetData, GetNotificationPreferencesApiV1NotificationsPreferencesGetResponses, GetOrganizationApiV1OrganizationsOrgIdGetData, GetOrganizationApiV1OrganizationsOrgIdGetErrors, GetOrganizationApiV1OrganizationsOrgIdGetResponses, GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetData, GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetErrors, GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetResponses, GetProjectByIdApiV1ProjectsProjectIdGetData, GetProjectByIdApiV1ProjectsProjectIdGetErrors, GetProjectByIdApiV1ProjectsProjectIdGetResponses, GetReleaseApiV1ReleasesIdGetData, GetReleaseApiV1ReleasesIdGetErrors, GetReleaseApiV1ReleasesIdGetResponses, GetResultDetailApiV1ReleasesIdResultsRidGetData, GetResultDetailApiV1ReleasesIdResultsRidGetErrors, GetResultDetailApiV1ReleasesIdResultsRidGetResponses, GetResultsApiV1ReleasesIdResultsGetData, GetResultsApiV1ReleasesIdResultsGetErrors, GetResultsApiV1ReleasesIdResultsGetResponses, GetTaskStatusApiV1TasksTaskIdGetData, GetTaskStatusApiV1TasksTaskIdGetErrors, GetTaskStatusApiV1TasksTaskIdGetResponses, GetTemplateApiV1TemplatesTemplateIdGetData, GetTemplateApiV1TemplatesTemplateIdGetErrors, GetTemplateApiV1TemplatesTemplateIdGetResponses, GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetData, GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetErrors, GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetResponses, HealthHealthGetData, HealthHealthGetResponses, ImportArtifactsApiV1ReleasesIdArtifactsImportPostData, ImportArtifactsApiV1ReleasesIdArtifactsImportPostErrors, ImportArtifactsApiV1ReleasesIdArtifactsImportPostResponses, InviteUserApiV1OrganizationsOrgIdUsersInvitePostData, InviteUserApiV1OrganizationsOrgIdUsersInvitePostErrors, InviteUserApiV1OrganizationsOrgIdUsersInvitePostResponses, ListAccessibleProjectsApiV1ProjectsGetData, ListAccessibleProjectsApiV1ProjectsGetErrors, ListAccessibleProjectsApiV1ProjectsGetResponses, ListApiKeysApiV1UsersUserIdApiKeysGetData, ListApiKeysApiV1UsersUserIdApiKeysGetErrors, ListApiKeysApiV1UsersUserIdApiKeysGetResponses, ListArtifactsApiV1ReleasesIdArtifactsGetData, ListArtifactsApiV1ReleasesIdArtifactsGetErrors, ListArtifactsApiV1ReleasesIdArtifactsGetResponses, ListConnectorsApiV1OrganizationsOrgIdConnectorsGetData, ListConnectorsApiV1OrganizationsOrgIdConnectorsGetErrors, ListConnectorsApiV1OrganizationsOrgIdConnectorsGetResponses, ListConnectorTypesApiV1ConnectorsTypesGetData, ListConnectorTypesApiV1ConnectorsTypesGetResponses, ListCustomRolesApiV1OrganizationsOrgIdRolesGetData, ListCustomRolesApiV1OrganizationsOrgIdRolesGetErrors, ListCustomRolesApiV1OrganizationsOrgIdRolesGetResponses, ListNotificationChannelsApiV1NotificationsChannelsGetData, ListNotificationChannelsApiV1NotificationsChannelsGetResponses, ListOrganizationsApiV1OrganizationsGetData, ListOrganizationsApiV1OrganizationsGetErrors, ListOrganizationsApiV1OrganizationsGetResponses, ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetData, ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetErrors, ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetResponses, ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetData, ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetErrors, ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetResponses, ListProfilesApiV1OrganizationsOrgIdProfilesGetData, ListProfilesApiV1OrganizationsOrgIdProfilesGetErrors, ListProfilesApiV1OrganizationsOrgIdProfilesGetResponses, ListReleasesApiV1ProjectsProjectIdReleasesGetData, ListReleasesApiV1ProjectsProjectIdReleasesGetErrors, ListReleasesApiV1ProjectsProjectIdReleasesGetResponses, ListTemplatesApiV1TemplatesGetData, ListTemplatesApiV1TemplatesGetErrors, ListTemplatesApiV1TemplatesGetResponses, LoginApiV1AuthLoginPostData, LoginApiV1AuthLoginPostErrors, LoginApiV1AuthLoginPostResponses, LogoutApiV1AuthLogoutPostData, LogoutApiV1AuthLogoutPostResponses, RefreshApiV1AuthRefreshPostData, RefreshApiV1AuthRefreshPostErrors, RefreshApiV1AuthRefreshPostResponses, RegisterApiV1AuthRegisterPostData, RegisterApiV1AuthRegisterPostErrors, RegisterApiV1AuthRegisterPostResponses, RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostData, RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostErrors, RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostResponses, ReloadCustomRulesApiV1AdminRulesReloadPostData, ReloadCustomRulesApiV1AdminRulesReloadPostResponses, RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteData, RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteErrors, RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteResponses, RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteData, RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteErrors, RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteResponses, RestoreOrganizationApiV1OrganizationsOrgIdRestorePostData, RestoreOrganizationApiV1OrganizationsOrgIdRestorePostErrors, RestoreOrganizationApiV1OrganizationsOrgIdRestorePostResponses, RestoreReleaseApiV1ReleasesIdRestorePostData, RestoreReleaseApiV1ReleasesIdRestorePostErrors, RestoreReleaseApiV1ReleasesIdRestorePostResponses, RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteData, RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteErrors, RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteResponses, SubscribeToEventApiV1NotificationsSubscriptionsPostData, SubscribeToEventApiV1NotificationsSubscriptionsPostErrors, SubscribeToEventApiV1NotificationsSubscriptionsPostResponses, TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostData, TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostErrors, TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostResponses, TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostData, TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostErrors, TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostResponses, UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteData, UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteErrors, UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteResponses, UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchData, UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchErrors, UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchResponses, UpdateCurrentUserProfileApiV1UsersMePatchData, UpdateCurrentUserProfileApiV1UsersMePatchErrors, UpdateCurrentUserProfileApiV1UsersMePatchResponses, UpdateCustomRoleApiV1RolesRoleIdPatchData, UpdateCustomRoleApiV1RolesRoleIdPatchErrors, UpdateCustomRoleApiV1RolesRoleIdPatchResponses, UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchData, UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchErrors, UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchResponses, UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchData, UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchErrors, UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchResponses, UpdateProfileApiV1ProfilesProfileIdPatchData, UpdateProfileApiV1ProfilesProfileIdPatchErrors, UpdateProfileApiV1ProfilesProfileIdPatchResponses, UpdateReleaseApiV1ReleasesIdPatchData, UpdateReleaseApiV1ReleasesIdPatchErrors, UpdateReleaseApiV1ReleasesIdPatchResponses, UpdateRuleApiV1RulesRuleIdPatchData, UpdateRuleApiV1RulesRuleIdPatchErrors, UpdateRuleApiV1RulesRuleIdPatchResponses, UpdateTemplateApiV1TemplatesTemplateIdPatchData, UpdateTemplateApiV1TemplatesTemplateIdPatchErrors, UpdateTemplateApiV1TemplatesTemplateIdPatchResponses, UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchData, UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchErrors, UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchResponses, VerifyReleaseApiV1ReleasesIdVerifyPostData, VerifyReleaseApiV1ReleasesIdVerifyPostErrors, VerifyReleaseApiV1ReleasesIdVerifyPostResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Login
 */
export const loginApiV1AuthLoginPost = <ThrowOnError extends boolean = false>(options: Options<LoginApiV1AuthLoginPostData, ThrowOnError>) => (options.client ?? client).post<LoginApiV1AuthLoginPostResponses, LoginApiV1AuthLoginPostErrors, ThrowOnError>({
    url: '/api/v1/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register
 */
export const registerApiV1AuthRegisterPost = <ThrowOnError extends boolean = false>(options: Options<RegisterApiV1AuthRegisterPostData, ThrowOnError>) => (options.client ?? client).post<RegisterApiV1AuthRegisterPostResponses, RegisterApiV1AuthRegisterPostErrors, ThrowOnError>({
    url: '/api/v1/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh
 */
export const refreshApiV1AuthRefreshPost = <ThrowOnError extends boolean = false>(options: Options<RefreshApiV1AuthRefreshPostData, ThrowOnError>) => (options.client ?? client).post<RefreshApiV1AuthRefreshPostResponses, RefreshApiV1AuthRefreshPostErrors, ThrowOnError>({
    url: '/api/v1/auth/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Logout
 */
export const logoutApiV1AuthLogoutPost = <ThrowOnError extends boolean = false>(options?: Options<LogoutApiV1AuthLogoutPostData, ThrowOnError>) => (options?.client ?? client).post<LogoutApiV1AuthLogoutPostResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/auth/logout',
    ...options
});

/**
 * List Organizations
 *
 * Endpoint para listar las organizaciones. Solo los usuarios con rol ADMIN pueden acceder a esta información.
 *
 * Atributos:
 * - skip: int - Número de registros a omitir para paginación.
 * - limit: int - Número máximo de registros a retornar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Una lista de diccionarios con la información de cada organización.
 * - Lanza HTTPException con status 403 si el usuario no tiene rol ADMIN.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const listOrganizationsApiV1OrganizationsGet = <ThrowOnError extends boolean = false>(options?: Options<ListOrganizationsApiV1OrganizationsGetData, ThrowOnError>) => (options?.client ?? client).get<ListOrganizationsApiV1OrganizationsGetResponses, ListOrganizationsApiV1OrganizationsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations',
    ...options
});

/**
 * Create Organization
 *
 * Endpoint para crear una nueva organización. Solo los usuarios con rol ADMIN pueden crear organizaciones.
 *
 * Atributos:
 * - payload: OrganizationCreateRequest - El cuerpo de la solicitud con los datos de la organización a crear.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información de la organización creada (id, name, slug).
 * - Lanza HTTPException con status 403 si el usuario no tiene rol ADMIN.
 * - Lanza HTTPException con status 409 si hay un error de validación (e.g., slug ya existe).
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const createOrganizationApiV1OrganizationsPost = <ThrowOnError extends boolean = false>(options: Options<CreateOrganizationApiV1OrganizationsPostData, ThrowOnError>) => (options.client ?? client).post<CreateOrganizationApiV1OrganizationsPostResponses, CreateOrganizationApiV1OrganizationsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Organization
 *
 * Endpoint para obtener los detalles de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información de la organización (id, name, slug, owner_id, etc.).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 404 si la organización no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const getOrganizationApiV1OrganizationsOrgIdGet = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationApiV1OrganizationsOrgIdGetData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationApiV1OrganizationsOrgIdGetResponses, GetOrganizationApiV1OrganizationsOrgIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}',
    ...options
});

/**
 * Get Project By Id
 */
export const getProjectByIdApiV1ProjectsProjectIdGet = <ThrowOnError extends boolean = false>(options: Options<GetProjectByIdApiV1ProjectsProjectIdGetData, ThrowOnError>) => (options.client ?? client).get<GetProjectByIdApiV1ProjectsProjectIdGetResponses, GetProjectByIdApiV1ProjectsProjectIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/projects/{project_id}',
    ...options
});

/**
 * List Accessible Projects
 */
export const listAccessibleProjectsApiV1ProjectsGet = <ThrowOnError extends boolean = false>(options?: Options<ListAccessibleProjectsApiV1ProjectsGetData, ThrowOnError>) => (options?.client ?? client).get<ListAccessibleProjectsApiV1ProjectsGetResponses, ListAccessibleProjectsApiV1ProjectsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/projects',
    ...options
});

/**
 * List Org Projects
 */
export const listOrgProjectsApiV1OrganizationsOrgIdProjectsGet = <ThrowOnError extends boolean = false>(options: Options<ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetData, ThrowOnError>) => (options.client ?? client).get<ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetResponses, ListOrgProjectsApiV1OrganizationsOrgIdProjectsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/projects',
    ...options
});

/**
 * Create Project
 *
 * Endpoint para crear un nuevo proyecto dentro de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: ProjectCreateRequest - El cuerpo de la solicitud con los datos del proyecto a crear.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información del proyecto creado (id, name).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 404 si la organización no existe.
 * - Lanza HTTPException con status 409 si hay un error de validación.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const createProjectApiV1OrganizationsOrgIdProjectsPost = <ThrowOnError extends boolean = false>(options: Options<CreateProjectApiV1OrganizationsOrgIdProjectsPostData, ThrowOnError>) => (options.client ?? client).post<CreateProjectApiV1OrganizationsOrgIdProjectsPostResponses, CreateProjectApiV1OrganizationsOrgIdProjectsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Project
 */
export const getProjectApiV1OrganizationsOrgIdProjectsProjectIdGet = <ThrowOnError extends boolean = false>(options: Options<GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetData, ThrowOnError>) => (options.client ?? client).get<GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetResponses, GetProjectApiV1OrganizationsOrgIdProjectsProjectIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/projects/{project_id}',
    ...options
});

/**
 * Archive Project
 *
 * Endpoint para archivar un proyecto.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - project_id: UUID - El ID del proyecto a archivar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con el ID y nombre del proyecto archivado.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 404 si la organización o el proyecto no existen.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const archiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePost = <ThrowOnError extends boolean = false>(options: Options<ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostData, ThrowOnError>) => (options.client ?? client).post<ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostResponses, ArchiveProjectApiV1OrganizationsOrgIdProjectsProjectIdArchivePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/projects/{project_id}/archive',
    ...options
});

/**
 * Restore Organization
 *
 * Endpoint para restaurar (desarchivar) una organización marcada como inactiva.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización a restaurar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con el ID y nombre de la organización restaurada.
 * - Lanza HTTPException con status 403 si el usuario no tiene rol U3.
 * - Lanza HTTPException con status 404 si la organización no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const restoreOrganizationApiV1OrganizationsOrgIdRestorePost = <ThrowOnError extends boolean = false>(options: Options<RestoreOrganizationApiV1OrganizationsOrgIdRestorePostData, ThrowOnError>) => (options.client ?? client).post<RestoreOrganizationApiV1OrganizationsOrgIdRestorePostResponses, RestoreOrganizationApiV1OrganizationsOrgIdRestorePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/restore',
    ...options
});

/**
 * Transfer Ownership
 *
 * Endpoint para transferir la propiedad de una organización a otro usuario.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: TransferOwnershipRequest - El cuerpo de la solicitud con el ID del nuevo propietario.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IOrganizationService - El servicio de organizaciones, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información de la organización actualizada.
 * - Lanza HTTPException con status 403 si el usuario no es el propietario actual.
 * - Lanza HTTPException con status 404 si la organización no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const transferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPost = <ThrowOnError extends boolean = false>(options: Options<TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostData, ThrowOnError>) => (options.client ?? client).post<TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostResponses, TransferOwnershipApiV1OrganizationsOrgIdTransferOwnershipPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/transfer-ownership',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Releases
 *
 * Lista todas las releases asociadas al proyecto especificado.
 *
 * Atributos:
 * - project_id: ID del proyecto del que se quieren listar las releases.
 * - project_access: Acceso validado al proyecto con datos del usuario autenticado.
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con la lista de releases si se encuentran exitosamente
 * - 403 Forbidden si el usuario no tiene acceso al proyecto
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const listReleasesApiV1ProjectsProjectIdReleasesGet = <ThrowOnError extends boolean = false>(options: Options<ListReleasesApiV1ProjectsProjectIdReleasesGetData, ThrowOnError>) => (options.client ?? client).get<ListReleasesApiV1ProjectsProjectIdReleasesGetResponses, ListReleasesApiV1ProjectsProjectIdReleasesGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/projects/{project_id}/releases',
    ...options
});

/**
 * Create Release
 *
 * Crea una nueva release dentro del proyecto especificado.
 *
 * Atributos:
 * - project_id: ID del proyecto al que pertenece la release.
 * - payload: Datos de la release a crear (nombre, versión, descripción).
 * - project_access: Acceso validado al proyecto con datos del usuario autenticado.
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 201 Created con el ID y estado inicial de la release si se crea exitosamente
 * - 403 Forbidden si el usuario no tiene permisos en el proyecto
 * - 422 Unprocessable Entity si los datos de entrada no son válidos
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const createReleaseApiV1ProjectsProjectIdReleasesPost = <ThrowOnError extends boolean = false>(options: Options<CreateReleaseApiV1ProjectsProjectIdReleasesPostData, ThrowOnError>) => (options.client ?? client).post<CreateReleaseApiV1ProjectsProjectIdReleasesPostResponses, CreateReleaseApiV1ProjectsProjectIdReleasesPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/projects/{project_id}/releases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Release
 *
 * Elimina una release específica por su ID.
 *
 * Atributos:
 * - id: ID de la release a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 204 No Content si se elimina exitosamente
 * - 403 Forbidden si el usuario no tiene acceso a la release
 * - 404 Not Found si la release no se encuentra
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const deleteReleaseApiV1ReleasesIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteReleaseApiV1ReleasesIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteReleaseApiV1ReleasesIdDeleteResponses, DeleteReleaseApiV1ReleasesIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}',
    ...options
});

/**
 * Get Release
 *
 * Obtiene los detalles de una release específica por su ID.
 *
 * Atributos:
 * - id: ID de la release a obtener.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con los detalles de la release si se encuentra exitosamente
 * - 403 Forbidden si el usuario no tiene acceso a la release
 * - 404 Not Found si la release no se encuentra
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const getReleaseApiV1ReleasesIdGet = <ThrowOnError extends boolean = false>(options: Options<GetReleaseApiV1ReleasesIdGetData, ThrowOnError>) => (options.client ?? client).get<GetReleaseApiV1ReleasesIdGetResponses, GetReleaseApiV1ReleasesIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}',
    ...options
});

/**
 * Update Release
 */
export const updateReleaseApiV1ReleasesIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateReleaseApiV1ReleasesIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateReleaseApiV1ReleasesIdPatchResponses, UpdateReleaseApiV1ReleasesIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive Release
 *
 * Archiva una release específica por su ID, cambiando su estado a 'ARCHIVADA'.
 *
 * Atributos:
 * - id: ID de la release a archivar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con un mensaje de éxito si se archiva exitosamente
 * - 403 Forbidden si el usuario no tiene permiso para archivar
 * - 404 Not Found si la release no se encuentra
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const archiveReleaseApiV1ReleasesIdArchivePost = <ThrowOnError extends boolean = false>(options: Options<ArchiveReleaseApiV1ReleasesIdArchivePostData, ThrowOnError>) => (options.client ?? client).post<ArchiveReleaseApiV1ReleasesIdArchivePostResponses, ArchiveReleaseApiV1ReleasesIdArchivePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/archive',
    ...options
});

/**
 * Restore Release
 *
 * Restaura una release archivada, cambiando su estado al anterior (BORRADOR).
 *
 * Atributos:
 * - id: ID de la release a restaurar.
 * - current_user: Usuario autenticado con rol U3 (reservado a administrador global).
 * - service: Instancia del servicio de releases (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con un mensaje de éxito si se restaura exitosamente
 * - 403 Forbidden si el usuario no tiene rol U3
 * - 404 Not Found si la release no se encuentra o no está archivada
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const restoreReleaseApiV1ReleasesIdRestorePost = <ThrowOnError extends boolean = false>(options: Options<RestoreReleaseApiV1ReleasesIdRestorePostData, ThrowOnError>) => (options.client ?? client).post<RestoreReleaseApiV1ReleasesIdRestorePostResponses, RestoreReleaseApiV1ReleasesIdRestorePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/restore',
    ...options
});

/**
 * List Artifacts
 *
 * Lista todos los artefactos asociados a una release específica por su ID.
 *
 * Atributos:
 * - id: ID de la release de la que se quieren listar los artefactos.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de artefactos (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con la lista de artefactos si se encuentran exitosamente
 * - 403 Forbidden si el usuario no tiene acceso a la release
 * - 404 Not Found si la release no se encuentra
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const listArtifactsApiV1ReleasesIdArtifactsGet = <ThrowOnError extends boolean = false>(options: Options<ListArtifactsApiV1ReleasesIdArtifactsGetData, ThrowOnError>) => (options.client ?? client).get<ListArtifactsApiV1ReleasesIdArtifactsGetResponses, ListArtifactsApiV1ReleasesIdArtifactsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/artifacts',
    ...options
});

/**
 * Add Artifact
 *
 * Agrega un nuevo artefacto a una release específica por su ID.
 *
 * Atributos:
 * - id: ID de la release a la que se quiere agregar el artefacto.
 * - payload: Datos del artefacto a agregar (tipo, conector, referencia externa
 * y descripción).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de artefactos (inyección de dependencias).
 *
 * Retorna:
 * - 201 Created con el ID del artefacto creado si se agrega exitosamente
 * - 403 Forbidden si el usuario no tiene acceso a la release
 * - 404 Not Found si la release no se encuentra
 * - 422 Unprocessable Entity si los datos de entrada no son válidos
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const addArtifactApiV1ReleasesIdArtifactsPost = <ThrowOnError extends boolean = false>(options: Options<AddArtifactApiV1ReleasesIdArtifactsPostData, ThrowOnError>) => (options.client ?? client).post<AddArtifactApiV1ReleasesIdArtifactsPostResponses, AddArtifactApiV1ReleasesIdArtifactsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/artifacts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove Artifact
 *
 * Elimina un artefacto específico de una release por sus IDs.
 *
 * Atributos:
 * - id: ID de la release de la que se quiere eliminar el artefacto.
 * - artifact_id: ID del artefacto a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de artefactos (inyección de dependencias).
 *
 * Retorna:
 * - 204 No Content si se elimina exitosamente
 * - 403 Forbidden si el usuario no tiene acceso a la release
 * - 404 Not Found si la release o el artefacto no se encuentran
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const removeArtifactApiV1ReleasesIdArtifactsArtifactIdDelete = <ThrowOnError extends boolean = false>(options: Options<RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteResponses, RemoveArtifactApiV1ReleasesIdArtifactsArtifactIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/artifacts/{artifact_id}',
    ...options
});

/**
 * Verify Release
 *
 * Lanza la verificación asíncrona.
 *
 * Atributos:
 * - id: ID de la release a verificar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 *
 * Retorna:
 * - 202 Accepted con el ID de la tarea de verificación y el nuevo estado de
 * la release si se lanza exitosamente
 * - 403 Forbidden si el usuario no tiene permiso para verificar
 * - 404 Not Found si la release no se encuentra
 * - 409 Conflict si la release no se encuentra en un estado válido para iniciar la verificación
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const verifyReleaseApiV1ReleasesIdVerifyPost = <ThrowOnError extends boolean = false>(options: Options<VerifyReleaseApiV1ReleasesIdVerifyPostData, ThrowOnError>) => (options.client ?? client).post<VerifyReleaseApiV1ReleasesIdVerifyPostResponses, VerifyReleaseApiV1ReleasesIdVerifyPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/verify',
    ...options
});

/**
 * Get Results
 *
 * Obtiene el historial paginado de verificaciones de esta release.
 *
 * Atributos:
 * - id: UUID - ID de la release de la que se quieren obtener las verificaciones.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con la lista paginada de verificaciones si se encuentran
 * - 403 Forbidden si el usuario no tiene acceso
 * - 404 Not Found si la release no se encuentra
 * - 500 Internal Server Error para cualquier error inesperado
 */
export const getResultsApiV1ReleasesIdResultsGet = <ThrowOnError extends boolean = false>(options: Options<GetResultsApiV1ReleasesIdResultsGetData, ThrowOnError>) => (options.client ?? client).get<GetResultsApiV1ReleasesIdResultsGetResponses, GetResultsApiV1ReleasesIdResultsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/results',
    ...options
});

/**
 * Get Result Detail
 *
 * Obtiene el informe detallado de una verificación individual.
 *
 * Atributos:
 * - id: UUID - ID de la release a la que pertenece la verificación.
 * - rid: UUID - ID de la verificación individual de la que se quieren obtener los detalles.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con el informe detallado de la verificación si se encuentra exitosamente
 * - 403 Forbidden si el usuario no tiene acceso
 * - 404 Not Found si la release o la verificación no se encuentran
 * - 500 Internal Server Error para cualquier error inesperado
 */
export const getResultDetailApiV1ReleasesIdResultsRidGet = <ThrowOnError extends boolean = false>(options: Options<GetResultDetailApiV1ReleasesIdResultsRidGetData, ThrowOnError>) => (options.client ?? client).get<GetResultDetailApiV1ReleasesIdResultsRidGetResponses, GetResultDetailApiV1ReleasesIdResultsRidGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/results/{rid}',
    ...options
});

/**
 * Get Verification Detail
 *
 * Obtiene el informe detallado de una verificación individual.
 *
 * Atributos:
 * - id: ID de la release a la que pertenece la verificación.
 * - verification_id: ID de la verificación individual de la que se quieren obtener los detalles.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con el informe detallado de la verificación si se encuentra exitosamente
 * - 403 Forbidden si el usuario no tiene acceso
 * - 404 Not Found si la release o la verificación no se encuentran
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const getVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGet = <ThrowOnError extends boolean = false>(options: Options<GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetData, ThrowOnError>) => (options.client ?? client).get<GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetResponses, GetVerificationDetailApiV1ReleasesIdVerificationsVerificationIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/verifications/{verification_id}',
    ...options
});

/**
 * Export Verification Result Pdf
 *
 * Exporta el resultado de una verificación a formato PDF.
 *
 * Atributos:
 * - id: ID de la release a la que pertenece la verificación.
 * - rid: ID de la verificación individual a exportar.
 * - format: Formato de exportación (solo PDF soportado actualmente).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 * - export_service: Instancia del servicio de exportación (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con el archivo PDF si se exporta exitosamente
 * - 400 Bad Request si el formato no es soportado
 * - 403 Forbidden si el usuario no tiene acceso
 * - 404 Not Found si la release o la verificación no se encuentran
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const exportVerificationResultPdfApiV1ReleasesIdResultsRidExportGet = <ThrowOnError extends boolean = false>(options: Options<ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetData, ThrowOnError>) => (options.client ?? client).get<ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetResponses, ExportVerificationResultPdfApiV1ReleasesIdResultsRidExportGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/results/{rid}/export',
    ...options
});

/**
 * Export Project Results Csv
 *
 * Exporta el historial de verificaciones de un proyecto a formato CSV.
 *
 * Atributos:
 * - project_id: ID del proyecto cuyas verificaciones se exportarán.
 * - format: Formato de exportación (solo CSV soportado actualmente).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de verificaciones (inyección de dependencias).
 * - export_service: Instancia del servicio de exportación (inyección de dependencias).
 *
 * Retorna:
 * - 200 OK con el archivo CSV si se exporta exitosamente
 * - 400 Bad Request si el formato no es soportado
 * - 403 Forbidden si el usuario no tiene acceso al proyecto
 * - 500 Internal Server Error para cualquier otro error inesperado
 */
export const exportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGet = <ThrowOnError extends boolean = false>(options: Options<ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetData, ThrowOnError>) => (options.client ?? client).get<ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetResponses, ExportProjectResultsCsvApiV1ProjectsProjectIdResultsExportGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/projects/{project_id}/results/export',
    ...options
});

/**
 * Import Artifacts
 *
 * Importa múltiples artefactos a una release desde un fichero CSV.
 *
 * Permite la importación masiva de artefactos a partir de un archivo CSV
 * proporcionado en la solicitud. La operación se ejecuta de forma asíncrona.
 *
 * Atributos:
 * - id: ID de la release a la que se importarán los artefactos.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Instancia del servicio de artefactos (inyección de dependencias).
 *
 * Retorna:
 * - 202 Accepted si la importación se inicia exitosamente.
 * - 403 Forbidden si el usuario no tiene acceso a la release.
 * - 404 Not Found si la release no se encuentra.
 * - 422 Unprocessable Entity si el fichero CSV no es válido.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const importArtifactsApiV1ReleasesIdArtifactsImportPost = <ThrowOnError extends boolean = false>(options: Options<ImportArtifactsApiV1ReleasesIdArtifactsImportPostData, ThrowOnError>) => (options.client ?? client).post<ImportArtifactsApiV1ReleasesIdArtifactsImportPostResponses, ImportArtifactsApiV1ReleasesIdArtifactsImportPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/releases/{id}/artifacts/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Connector Types
 *
 * Endpoint para listar los tipos de conectores disponibles y sus implementaciones.
 *
 * Retorna la lista de todas las implementaciones de conectores registradas,
 * agrupadas por tipo de conector, junto con sus metadatos y esquemas de
 * configuración.
 *
 * Atributos:
 * - current_user: Usuario autenticado con permisos del token JWT.
 *
 * Retorna:
 * - Diccionario con la lista de implementaciones y la agrupación por tipo.
 * - 401 Unauthorized si el token es inválido.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const listConnectorTypesApiV1ConnectorsTypesGet = <ThrowOnError extends boolean = false>(options?: Options<ListConnectorTypesApiV1ConnectorsTypesGetData, ThrowOnError>) => (options?.client ?? client).get<ListConnectorTypesApiV1ConnectorsTypesGetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/connectors/types',
    ...options
});

/**
 * List Connectors
 *
 * Endpoint para listar los conectores de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - active_only: bool - Si se deben mostrar solo los conectores activos.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IConnectorService - El servicio de conectores, inyectado mediante dependencias.
 *
 * Retorna:
 * - Una lista de diccionarios con la información de cada conector.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const listConnectorsApiV1OrganizationsOrgIdConnectorsGet = <ThrowOnError extends boolean = false>(options: Options<ListConnectorsApiV1OrganizationsOrgIdConnectorsGetData, ThrowOnError>) => (options.client ?? client).get<ListConnectorsApiV1OrganizationsOrgIdConnectorsGetResponses, ListConnectorsApiV1OrganizationsOrgIdConnectorsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/connectors',
    ...options
});

/**
 * Register Connector
 *
 * Endpoint para registrar un nuevo conector en una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: ConnectorCreateRequest - El cuerpo de la solicitud, que incluye el tipo, nombre y configuración del conector.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IConnectorService - El servicio de conectores, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con el ID, nombre y estado del conector registrado.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 422 si los datos de entrada son inválidos.
 * - Lanza HTTPException con status 500 para cualquier otro error inesperado.
 */
export const registerConnectorApiV1OrganizationsOrgIdConnectorsPost = <ThrowOnError extends boolean = false>(options: Options<RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostData, ThrowOnError>) => (options.client ?? client).post<RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostResponses, RegisterConnectorApiV1OrganizationsOrgIdConnectorsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/connectors',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Connector
 *
 * Endpoint para eliminar un conector existente.
 *
 * Atributos:
 * - connector_id: UUID - El ID del conector a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IConnectorService - El servicio de conectores, inyectado mediante dependencias.
 *
 * Retorna:
 * - No retorna contenido (204) si la eliminación es exitosa.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el conector no es encontrado.
 * - Lanza HTTPException con status 500 para cualquier otro error inesperado.
 */
export const deleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteResponses, DeleteConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/connectors/{connector_id}',
    ...options
});

/**
 * Update Connector
 *
 * Endpoint para actualizar un conector existente.
 *
 * Atributos:
 * - connector_id: UUID - El ID del conector a actualizar.
 * - payload: ConnectorUpdateRequest - El cuerpo de la solicitud, que incluye el nombre y configuración actualizados del conector.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IConnectorService - El servicio de conectores, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con el ID, nombre y estado del conector actualizado.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el conector no es encontrado.
 * - Lanza HTTPException con status 422 si los datos de entrada son inválidos.
 * - Lanza HTTPException con status 500 para cualquier otro error inesperado.
 */
export const updateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchResponses, UpdateConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/connectors/{connector_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test Connector
 *
 * Endpoint para probar la conexión de un conector existente.
 *
 * Atributos:
 * - connector_id: UUID - El ID del conector a probar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IConnectorService - El servicio de conectores, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con el resultado de la prueba y un mensaje informativo.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el conector no es encontrado.
 * - Lanza HTTPException con status 409 si la conexión falla.
 * - Lanza HTTPException con status 500 para cualquier otro error inesperado.
 */
export const testConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPost = <ThrowOnError extends boolean = false>(options: Options<TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostData, ThrowOnError>) => (options.client ?? client).post<TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostResponses, TestConnectorApiV1OrganizationsOrgIdConnectorsConnectorIdTestPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/connectors/{connector_id}/test',
    ...options
});

/**
 * List Profiles
 *
 * Endpoint para listar los perfiles de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - skip: int - Número de registros a omitir para paginación.
 * - limit: int - Número máximo de registros a retornar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias
 *
 * Retorna:
 * - Una lista de diccionarios con la información de cada perfil, incluyendo el conteo de reglas asociadas.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const listProfilesApiV1OrganizationsOrgIdProfilesGet = <ThrowOnError extends boolean = false>(options: Options<ListProfilesApiV1OrganizationsOrgIdProfilesGetData, ThrowOnError>) => (options.client ?? client).get<ListProfilesApiV1OrganizationsOrgIdProfilesGetResponses, ListProfilesApiV1OrganizationsOrgIdProfilesGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/profiles',
    ...options
});

/**
 * Create Profile
 *
 * Endpoint para crear un nuevo perfil dentro de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: ProfileCreateRequest - El cuerpo de la solicitud con los datos del perfil a crear.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información del perfil creado (id, name, is_default).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso a la organización.
 * - Lanza HTTPException con status 409 si hay un error de validación.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const createProfileApiV1OrganizationsOrgIdProfilesPost = <ThrowOnError extends boolean = false>(options: Options<CreateProfileApiV1OrganizationsOrgIdProfilesPostData, ThrowOnError>) => (options.client ?? client).post<CreateProfileApiV1OrganizationsOrgIdProfilesPostResponses, CreateProfileApiV1OrganizationsOrgIdProfilesPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/profiles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Profile
 *
 * Endpoint para eliminar un perfil existente.
 *
 * Atributos:
 * - profile_id: UUID - El ID del perfil a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias.
 * Retorna:
 * - No retorna contenido (204 No Content) si la eliminación es exitosa.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el perfil no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const deleteProfileApiV1ProfilesProfileIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteProfileApiV1ProfilesProfileIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteProfileApiV1ProfilesProfileIdDeleteResponses, DeleteProfileApiV1ProfilesProfileIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}',
    ...options
});

/**
 * Update Profile
 *
 * Endpoint para actualizar un perfil existente.
 *
 * Atributos:
 * - profile_id: UUID - El ID del perfil a actualizar.
 * - payload: ProfileUpdateRequest - El cuerpo de la solicitud con los datos del perfil a actualizar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información del perfil actualizado (id, name, is_default).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el perfil no existe.
 * - Lanza HTTPException con status 409 si hay un error de validación.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const updateProfileApiV1ProfilesProfileIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateProfileApiV1ProfilesProfileIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateProfileApiV1ProfilesProfileIdPatchResponses, UpdateProfileApiV1ProfilesProfileIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add Rule
 *
 * Endpoint para agregar una nueva regla a un perfil existente.
 *
 * Atributos:
 * - profile_id: UUID - El ID del perfil al que se agregará la regla.
 * - payload: RuleCreateRequest - El cuerpo de la solicitud con los datos de la regla a crear.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información de la regla creada (id, rule_template).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si el perfil no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const addRuleApiV1ProfilesProfileIdRulesPost = <ThrowOnError extends boolean = false>(options: Options<AddRuleApiV1ProfilesProfileIdRulesPostData, ThrowOnError>) => (options.client ?? client).post<AddRuleApiV1ProfilesProfileIdRulesPostResponses, AddRuleApiV1ProfilesProfileIdRulesPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}/rules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Rule
 *
 * Endpoint para eliminar una regla existente.
 *
 * Atributos:
 * - rule_id: UUID - El ID de la regla a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias
 *
 * Retorna:
 * - No retorna contenido (204 No Content) si la eliminación es exitosa.
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si la regla no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const deleteRuleApiV1RulesRuleIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteRuleApiV1RulesRuleIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteRuleApiV1RulesRuleIdDeleteResponses, DeleteRuleApiV1RulesRuleIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/rules/{rule_id}',
    ...options
});

/**
 * Update Rule
 *
 * Endpoint para actualizar una regla existente.
 *
 * Atributos:
 * - rule_id: UUID - El ID de la regla a actualizar.
 * - payload: RuleUpdateRequest - El cuerpo de la solicitud con los datos de la regla a actualizar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: IProfileService - El servicio de perfiles, inyectado mediante dependencias.
 *
 * Retorna:
 * - Un diccionario con la información de la regla actualizada (id, is_active).
 * - Lanza HTTPException con status 403 si el usuario no tiene acceso.
 * - Lanza HTTPException con status 404 si la regla no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const updateRuleApiV1RulesRuleIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateRuleApiV1RulesRuleIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateRuleApiV1RulesRuleIdPatchResponses, UpdateRuleApiV1RulesRuleIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/rules/{rule_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Task Status
 *
 * Endpoint para obtener el estado de una tarea asíncrona.
 *
 * Atributos:
 * - task_id: str - El ID de la tarea a consultar.
 * - service: ITaskService - El servicio de tareas, inyectado mediante dependencias
 *
 * Retorna:
 * - Un diccionario con el ID de la tarea, su estado actual y el resultado si está disponible.
 * - Lanza HTTPException con status 404 si la tarea no existe.
 * - Lanza HTTPException con status 500 para cualquier error inesperado.
 */
export const getTaskStatusApiV1TasksTaskIdGet = <ThrowOnError extends boolean = false>(options: Options<GetTaskStatusApiV1TasksTaskIdGetData, ThrowOnError>) => (options.client ?? client).get<GetTaskStatusApiV1TasksTaskIdGetResponses, GetTaskStatusApiV1TasksTaskIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/tasks/{task_id}',
    ...options
});

/**
 * Get Current User Profile
 *
 * Obtiene el perfil del usuario autenticado actualmente.
 *
 * Atributos:
 * - current_user: Usuario autenticado (del token JWT).
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Perfil del usuario con id, email, display_name, role y organization_id.
 * - 401 Unauthorized si el token es inválido.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const getCurrentUserProfileApiV1UsersMeGet = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserProfileApiV1UsersMeGetData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserProfileApiV1UsersMeGetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me',
    ...options
});

/**
 * Update Current User Profile
 *
 * Actualiza el perfil del usuario autenticado (display_name).
 *
 * Atributos:
 * - payload: Datos a actualizar (display_name).
 * - current_user: Usuario autenticado (del token JWT).
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Perfil actualizado del usuario.
 * - 400 Bad Request si los datos son inválidos.
 * - 401 Unauthorized si el token es inválido.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const updateCurrentUserProfileApiV1UsersMePatch = <ThrowOnError extends boolean = false>(options: Options<UpdateCurrentUserProfileApiV1UsersMePatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateCurrentUserProfileApiV1UsersMePatchResponses, UpdateCurrentUserProfileApiV1UsersMePatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Change Password
 *
 * Cambia la contraseña del usuario autenticado.
 *
 * Atributos:
 * - payload: Contraseña actual y nueva contraseña.
 * - current_user: Usuario autenticado (del token JWT).
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Mensaje de éxito si la contraseña se cambió correctamente.
 * - 400 Bad Request si la contraseña actual es incorrecta.
 * - 401 Unauthorized si el token es inválido.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const changePasswordApiV1UsersMePasswordPost = <ThrowOnError extends boolean = false>(options: Options<ChangePasswordApiV1UsersMePasswordPostData, ThrowOnError>) => (options.client ?? client).post<ChangePasswordApiV1UsersMePasswordPostResponses, ChangePasswordApiV1UsersMePasswordPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me/password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete User Account
 */
export const deleteUserAccountApiV1UsersMeAccountDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteUserAccountApiV1UsersMeAccountDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteUserAccountApiV1UsersMeAccountDeleteResponses, DeleteUserAccountApiV1UsersMeAccountDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me/account',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Export User Data
 *
 * GDPR Art.20 — Export all personal data for the authenticated user.
 */
export const exportUserDataApiV1UsersMeExportGet = <ThrowOnError extends boolean = false>(options?: Options<ExportUserDataApiV1UsersMeExportGetData, ThrowOnError>) => (options?.client ?? client).get<ExportUserDataApiV1UsersMeExportGetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/me/export',
    ...options
});

/**
 * List Organization Users
 *
 * Lista los usuarios de una organización.
 *
 * Solo accesible por usuarios con rol MANAGER o superior que pertenezcan a la organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - skip: int - Número de registros a omitir para paginación.
 * - limit: int - Número máximo de registros a retornar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Lista de usuarios con su id, email, display_name y role.
 * - 403 Forbidden si el usuario no tiene acceso a la organización.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const listOrganizationUsersApiV1OrganizationsOrgIdUsersGet = <ThrowOnError extends boolean = false>(options: Options<ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetData, ThrowOnError>) => (options.client ?? client).get<ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetResponses, ListOrganizationUsersApiV1OrganizationsOrgIdUsersGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/users',
    ...options
});

/**
 * Invite User
 *
 * Invita a un usuario a unirse a una organización.
 *
 * Solo accesible por usuarios con rol MANAGER o superior. El usuario recibe
 * un email con un enlace para completar su registro.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: Datos del usuario a invitar (email y rol).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Mensaje de éxito con el email del usuario invitado.
 * - 403 Forbidden si el usuario no tiene permisos.
 * - 409 Conflict si el email ya está registrado.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const inviteUserApiV1OrganizationsOrgIdUsersInvitePost = <ThrowOnError extends boolean = false>(options: Options<InviteUserApiV1OrganizationsOrgIdUsersInvitePostData, ThrowOnError>) => (options.client ?? client).post<InviteUserApiV1OrganizationsOrgIdUsersInvitePostResponses, InviteUserApiV1OrganizationsOrgIdUsersInvitePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/users/invite',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update User Role
 *
 * Actualiza el rol de un usuario dentro de una organización.
 *
 * Solo accesible por usuarios con rol MANAGER o superior.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - user_id: UUID - El ID del usuario cuyo rol se actualizará.
 * - payload: Nuevo rol para el usuario.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Usuario actualizado con su nuevo rol.
 * - 403 Forbidden si el usuario no tiene permisos o intenta cambiar el rol del Owner.
 * - 404 Not Found si el usuario no existe.
 * - 500 Internal Server Error para cualquier otro error inesperado.
 */
export const updateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatch = <ThrowOnError extends boolean = false>(options: Options<UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchResponses, UpdateUserRoleApiV1OrganizationsOrgIdUsersUserIdRolePatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/users/{user_id}/role',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove User From Org
 *
 * Elimina a un usuario de una organización.
 *
 * El usuario podrá seguir usando el sistema pero perderá acceso a la organización.
 * No es posible eliminar al Owner de la organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - user_id: UUID - El ID del usuario a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - 204 No Content si el usuario fue eliminado correctamente.
 * - 403 Forbidden si el usuario no tiene permisos o es el Owner.
 * - 404 Not Found si el usuario no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const removeUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDelete = <ThrowOnError extends boolean = false>(options: Options<RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteResponses, RemoveUserFromOrgApiV1OrganizationsOrgIdUsersUserIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/users/{user_id}',
    ...options
});

/**
 * Admin List Users
 *
 * Lista todos los usuarios del sistema con filtros (solo U3).
 *
 * Atributos:
 * - skip: int - Número de registros a omitir para paginación.
 * - limit: int - Número máximo de registros a retornar.
 * - is_active: bool - Filtrar por estado de activación.
 * - role: UserRole - Filtrar por rol global.
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - Lista de usuarios con su información.
 * - 403 Forbidden si el usuario no es U3.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const adminListUsersApiV1AdminUsersGet = <ThrowOnError extends boolean = false>(options?: Options<AdminListUsersApiV1AdminUsersGetData, ThrowOnError>) => (options?.client ?? client).get<AdminListUsersApiV1AdminUsersGetResponses, AdminListUsersApiV1AdminUsersGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users',
    ...options
});

/**
 * Admin Create User
 *
 * Crea un nuevo usuario en el sistema (solo U3).
 *
 * Atributos:
 * - payload: Datos del usuario a crear.
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con la información del usuario creado.
 * - 403 Forbidden si el usuario no es U3.
 * - 409 Conflict si el email ya existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const adminCreateUserApiV1AdminUsersPost = <ThrowOnError extends boolean = false>(options: Options<AdminCreateUserApiV1AdminUsersPostData, ThrowOnError>) => (options.client ?? client).post<AdminCreateUserApiV1AdminUsersPostResponses, AdminCreateUserApiV1AdminUsersPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Admin Activate User
 *
 * Activa una cuenta de usuario (solo U3).
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario a activar.
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con el usuario activado.
 * - 403 Forbidden si el usuario no es U3.
 * - 404 Not Found si el usuario no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const adminActivateUserApiV1AdminUsersUserIdActivatePatch = <ThrowOnError extends boolean = false>(options: Options<AdminActivateUserApiV1AdminUsersUserIdActivatePatchData, ThrowOnError>) => (options.client ?? client).patch<AdminActivateUserApiV1AdminUsersUserIdActivatePatchResponses, AdminActivateUserApiV1AdminUsersUserIdActivatePatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users/{user_id}/activate',
    ...options
});

/**
 * Admin Deactivate User
 *
 * Desactiva una cuenta de usuario (solo U3).
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario a desactivar.
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con el usuario desactivado.
 * - 403 Forbidden si el usuario no es U3 o intenta desactivarse a sí mismo.
 * - 404 Not Found si el usuario no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const adminDeactivateUserApiV1AdminUsersUserIdDeactivatePatch = <ThrowOnError extends boolean = false>(options: Options<AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchData, ThrowOnError>) => (options.client ?? client).patch<AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchResponses, AdminDeactivateUserApiV1AdminUsersUserIdDeactivatePatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users/{user_id}/deactivate',
    ...options
});

/**
 * Admin Update Global Role
 *
 * Actualiza el rol global de un usuario (solo U3).
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario cuyo rol se actualizará.
 * - payload: Nuevo rol global para el usuario.
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de usuarios inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con el usuario actualizado.
 * - 403 Forbidden si el usuario no es U3 o intenta cambiar su propio rol.
 * - 404 Not Found si el usuario no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const adminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatch = <ThrowOnError extends boolean = false>(options: Options<AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchData, ThrowOnError>) => (options.client ?? client).patch<AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchResponses, AdminUpdateGlobalRoleApiV1AdminUsersUserIdRolePatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users/{user_id}/role',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Custom Roles
 *
 * Lista los roles personalizados de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de roles personalizado inyectado.
 *
 * Retorna:
 * - Lista de roles personalizados con sus permisos.
 * - 403 Forbidden si el usuario no tiene acceso.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const listCustomRolesApiV1OrganizationsOrgIdRolesGet = <ThrowOnError extends boolean = false>(options: Options<ListCustomRolesApiV1OrganizationsOrgIdRolesGetData, ThrowOnError>) => (options.client ?? client).get<ListCustomRolesApiV1OrganizationsOrgIdRolesGetResponses, ListCustomRolesApiV1OrganizationsOrgIdRolesGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/roles',
    ...options
});

/**
 * Create Custom Role
 *
 * Crea un rol personalizado dentro de una organización.
 *
 * Los roles personalizados permiten definir combinaciones específicas de permisos
 * que pueden asignarse a usuarios dentro de una organización.
 *
 * Atributos:
 * - org_id: UUID - El ID de la organización.
 * - payload: Datos del rol a crear (nombre y lista de permisos).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de roles personalizado inyectado.
 *
 * Retorna:
 * - El rol personalizado creado con sus permisos.
 * - 403 Forbidden si el usuario no tiene acceso.
 * - 409 Conflict si ya existe un rol con ese nombre.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const createCustomRoleApiV1OrganizationsOrgIdRolesPost = <ThrowOnError extends boolean = false>(options: Options<CreateCustomRoleApiV1OrganizationsOrgIdRolesPostData, ThrowOnError>) => (options.client ?? client).post<CreateCustomRoleApiV1OrganizationsOrgIdRolesPostResponses, CreateCustomRoleApiV1OrganizationsOrgIdRolesPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/organizations/{org_id}/roles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Custom Role
 *
 * Elimina un rol personalizado.
 *
 * Los usuarios que tengan este rol asignado conservarán sus permisos base
 * pero perderán los permisos específicos del rol eliminado.
 *
 * Atributos:
 * - role_id: UUID - El ID del rol a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de roles personalizado inyectado.
 *
 * Retorna:
 * - 204 No Content si el rol fue eliminado correctamente.
 * - 403 Forbidden si el usuario no tiene acceso.
 * - 404 Not Found si el rol no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const deleteCustomRoleApiV1RolesRoleIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteCustomRoleApiV1RolesRoleIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteCustomRoleApiV1RolesRoleIdDeleteResponses, DeleteCustomRoleApiV1RolesRoleIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/roles/{role_id}',
    ...options
});

/**
 * Update Custom Role
 *
 * Actualiza un rol personalizado existente.
 *
 * Atributos:
 * - role_id: UUID - El ID del rol a actualizar.
 * - payload: Datos actualizados del rol.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de roles personalizado inyectado.
 *
 * Retorna:
 * - El rol personalizado actualizado.
 * - 403 Forbidden si el usuario no tiene acceso.
 * - 404 Not Found si el rol no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const updateCustomRoleApiV1RolesRoleIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateCustomRoleApiV1RolesRoleIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateCustomRoleApiV1RolesRoleIdPatchResponses, UpdateCustomRoleApiV1RolesRoleIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/roles/{role_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Dashboard Metrics
 *
 * Endpoint para obtener métricas del dashboard.
 *
 * Si se proporciona org_id, retorna métricas de esa organización (requiere acceso).
 * Si no se proporciona org_id, retorna métricas basadas en las organizaciones
 * a las que el usuario tiene acceso.
 *
 * Atributos:
 * - org_id: UUID opcional - Filtrar por ID de organización.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - release_repo: Repositorio de releases inyectado.
 * - verification_repo: Repositorio de resultados de verificación inyectado.
 *
 * Retorna:
 * - Métricas agregadas del dashboard incluyendo totales de releases,
 * verificaciones y tasa de aprobación.
 */
export const getDashboardMetricsApiV1DashboardMetricsGet = <ThrowOnError extends boolean = false>(options?: Options<GetDashboardMetricsApiV1DashboardMetricsGetData, ThrowOnError>) => (options?.client ?? client).get<GetDashboardMetricsApiV1DashboardMetricsGetResponses, GetDashboardMetricsApiV1DashboardMetricsGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/dashboard/metrics',
    ...options
});

/**
 * List Api Keys
 *
 * Lista las API keys del usuario autenticado.
 *
 * Solo el propio usuario puede ver sus API keys. La clave completa solo se
 * muestra en el momento de la creación; este endpoint retorna solo el prefijo.
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario cuyas API keys se listan.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - api_key_repo: Repositorio de API keys inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con la lista de API keys del usuario.
 * - 403 Forbidden si el usuario intenta ver las API keys de otro usuario.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const listApiKeysApiV1UsersUserIdApiKeysGet = <ThrowOnError extends boolean = false>(options: Options<ListApiKeysApiV1UsersUserIdApiKeysGetData, ThrowOnError>) => (options.client ?? client).get<ListApiKeysApiV1UsersUserIdApiKeysGetResponses, ListApiKeysApiV1UsersUserIdApiKeysGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}/api-keys',
    ...options
});

/**
 * Create Api Key
 *
 * Crea una nueva API key para el usuario autenticado.
 *
 * La API key puede tener una fecha de expiración opcional. Solo el propio
 * usuario puede crear API keys para su cuenta.
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario para el que se crea la API key.
 * - payload: Datos de la API key (nombre y días de expiración opcionales).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - api_key_repo: Repositorio de API keys inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con los datos de la API key creada (incluyendo la clave completa).
 * - 400 Bad Request si el usuario no pertenece a una organización.
 * - 403 Forbidden si el usuario intenta crear una API key para otro usuario.
 * - 409 Conflict si los datos de la API key no son válidos.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const createApiKeyApiV1UsersUserIdApiKeysPost = <ThrowOnError extends boolean = false>(options: Options<CreateApiKeyApiV1UsersUserIdApiKeysPostData, ThrowOnError>) => (options.client ?? client).post<CreateApiKeyApiV1UsersUserIdApiKeysPostResponses, CreateApiKeyApiV1UsersUserIdApiKeysPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}/api-keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke Api Key
 *
 * Revoca (invalida) una API key del usuario autenticado.
 *
 * Una vez revocada, la API key ya no podrá usarse para autenticar solicitudes.
 * Solo el propio usuario puede revocar sus API keys.
 *
 * Atributos:
 * - user_id: UUID - El ID del usuario propietario de la API key.
 * - key_id: UUID - El ID de la API key a revocar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - api_key_repo: Repositorio de API keys inyectado mediante dependencias.
 *
 * Retorna:
 * - 204 No Content si la API key fue revocada correctamente.
 * - 403 Forbidden si el usuario intenta revocar una API key de otro usuario.
 * - 404 Not Found si la API key no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const revokeApiKeyApiV1UsersUserIdApiKeysKeyIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteResponses, RevokeApiKeyApiV1UsersUserIdApiKeysKeyIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/users/{user_id}/api-keys/{key_id}',
    ...options
});

/**
 * List Templates
 *
 * Lista las plantillas de release accesibles por el usuario.
 *
 * Atributos:
 * - skip: int - Número de registros a omitir para paginación.
 * - limit: int - Número máximo de registros a retornar.
 * - include_archived: bool - Incluir plantillas archivadas.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - Lista de plantillas con su información.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const listTemplatesApiV1TemplatesGet = <ThrowOnError extends boolean = false>(options?: Options<ListTemplatesApiV1TemplatesGetData, ThrowOnError>) => (options?.client ?? client).get<ListTemplatesApiV1TemplatesGetResponses, ListTemplatesApiV1TemplatesGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates',
    ...options
});

/**
 * Create Template
 *
 * Crea una nueva plantilla de release.
 *
 * Atributos:
 * - payload: Datos de la plantilla a crear (nombre, descripción, profile_id).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con el ID de la plantilla creada.
 * - 403 Forbidden si el usuario no tiene permisos.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const createTemplateApiV1TemplatesPost = <ThrowOnError extends boolean = false>(options: Options<CreateTemplateApiV1TemplatesPostData, ThrowOnError>) => (options.client ?? client).post<CreateTemplateApiV1TemplatesPostResponses, CreateTemplateApiV1TemplatesPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Template
 *
 * Obtiene los detalles de una plantilla específica.
 *
 * Atributos:
 * - template_id: UUID - El ID de la plantilla.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - Detalles de la plantilla.
 * - 404 Not Found si la plantilla no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const getTemplateApiV1TemplatesTemplateIdGet = <ThrowOnError extends boolean = false>(options: Options<GetTemplateApiV1TemplatesTemplateIdGetData, ThrowOnError>) => (options.client ?? client).get<GetTemplateApiV1TemplatesTemplateIdGetResponses, GetTemplateApiV1TemplatesTemplateIdGetErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates/{template_id}',
    ...options
});

/**
 * Update Template
 *
 * Actualiza una plantilla de release.
 *
 * Atributos:
 * - template_id: UUID - El ID de la plantilla a actualizar.
 * - payload: Datos actualizados de la plantilla.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con la plantilla actualizada.
 * - 404 Not Found si la plantilla no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const updateTemplateApiV1TemplatesTemplateIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateTemplateApiV1TemplatesTemplateIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateTemplateApiV1TemplatesTemplateIdPatchResponses, UpdateTemplateApiV1TemplatesTemplateIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates/{template_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive Template
 *
 * Archiva una plantilla de release.
 *
 * Atributos:
 * - template_id: UUID - El ID de la plantilla a archivar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con mensaje de éxito.
 * - 404 Not Found si la plantilla no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const archiveTemplateApiV1TemplatesTemplateIdArchivePost = <ThrowOnError extends boolean = false>(options: Options<ArchiveTemplateApiV1TemplatesTemplateIdArchivePostData, ThrowOnError>) => (options.client ?? client).post<ArchiveTemplateApiV1TemplatesTemplateIdArchivePostResponses, ArchiveTemplateApiV1TemplatesTemplateIdArchivePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates/{template_id}/archive',
    ...options
});

/**
 * Clone Template
 *
 * Clona una plantilla de release a otra organización.
 *
 * Atributos:
 * - template_id: UUID - El ID de la plantilla a clonar.
 * - payload: Datos para la clonación (nombre destino, organización destino).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de plantillas inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con el ID de la nueva plantilla clonada.
 * - 404 Not Found si la plantilla no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const cloneTemplateApiV1TemplatesTemplateIdClonePost = <ThrowOnError extends boolean = false>(options: Options<CloneTemplateApiV1TemplatesTemplateIdClonePostData, ThrowOnError>) => (options.client ?? client).post<CloneTemplateApiV1TemplatesTemplateIdClonePostResponses, CloneTemplateApiV1TemplatesTemplateIdClonePostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/templates/{template_id}/clone',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Notification Channels
 *
 * Lista los canales de notificación disponibles y su configuración.
 *
 * Atributos:
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - Lista de canales de notificación configurados para la organización.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const listNotificationChannelsApiV1NotificationsChannelsGet = <ThrowOnError extends boolean = false>(options?: Options<ListNotificationChannelsApiV1NotificationsChannelsGetData, ThrowOnError>) => (options?.client ?? client).get<ListNotificationChannelsApiV1NotificationsChannelsGetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/channels',
    ...options
});

/**
 * Configure Notification Channel
 *
 * Configura un nuevo canal de notificación para la organización.
 *
 * Atributos:
 * - payload: Configuración del canal (tipo, habilitado, datos específicos).
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con el ID del canal configurado.
 * - 403 Forbidden si el usuario no tiene permisos.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const configureNotificationChannelApiV1NotificationsChannelsPost = <ThrowOnError extends boolean = false>(options: Options<ConfigureNotificationChannelApiV1NotificationsChannelsPostData, ThrowOnError>) => (options.client ?? client).post<ConfigureNotificationChannelApiV1NotificationsChannelsPostResponses, ConfigureNotificationChannelApiV1NotificationsChannelsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/channels',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Notification Channel
 *
 * Elimina un canal de notificación.
 *
 * Atributos:
 * - channel_id: UUID - El ID del canal a eliminar.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 204 No Content si el canal fue eliminado correctamente.
 * - 404 Not Found si el canal no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const deleteNotificationChannelApiV1NotificationsChannelsChannelIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteResponses, DeleteNotificationChannelApiV1NotificationsChannelsChannelIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/channels/{channel_id}',
    ...options
});

/**
 * Update Notification Channel
 *
 * Actualiza la configuración de un canal de notificación.
 *
 * Atributos:
 * - channel_id: UUID - El ID del canal a actualizar.
 * - payload: Nueva configuración del canal.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con el canal actualizado.
 * - 404 Not Found si el canal no existe.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const updateNotificationChannelApiV1NotificationsChannelsChannelIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchResponses, UpdateNotificationChannelApiV1NotificationsChannelsChannelIdPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/channels/{channel_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Notification Preferences
 *
 * Obtiene las preferencias de notificación del usuario actual.
 *
 * Atributos:
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - Preferencias de notificación del usuario.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const getNotificationPreferencesApiV1NotificationsPreferencesGet = <ThrowOnError extends boolean = false>(options?: Options<GetNotificationPreferencesApiV1NotificationsPreferencesGetData, ThrowOnError>) => (options?.client ?? client).get<GetNotificationPreferencesApiV1NotificationsPreferencesGetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/preferences',
    ...options
});

/**
 * Update Notification Preferences
 *
 * Actualiza las preferencias de notificación del usuario actual.
 *
 * Atributos:
 * - payload: Nuevas preferencias de notificación.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con las preferencias actualizadas.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const updateNotificationPreferencesApiV1NotificationsPreferencesPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchData, ThrowOnError>) => (options.client ?? client).patch<UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchResponses, UpdateNotificationPreferencesApiV1NotificationsPreferencesPatchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/preferences',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Subscribe To Event
 *
 * Suscribe al usuario a un tipo de evento de notificación.
 *
 * Atributos:
 * - payload: Tipo de evento y habilitación.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 201 Created con la suscripción creada.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const subscribeToEventApiV1NotificationsSubscriptionsPost = <ThrowOnError extends boolean = false>(options: Options<SubscribeToEventApiV1NotificationsSubscriptionsPostData, ThrowOnError>) => (options.client ?? client).post<SubscribeToEventApiV1NotificationsSubscriptionsPostResponses, SubscribeToEventApiV1NotificationsSubscriptionsPostErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/subscriptions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unsubscribe From Event
 *
 * Cancela la suscripción del usuario a un tipo de evento.
 *
 * Atributos:
 * - event_type: str - El tipo de evento al que desea desuscribirse.
 * - current_user: Usuario autenticado con permisos del token JWT.
 * - service: Servicio de notificaciones inyectado mediante dependencias.
 *
 * Retorna:
 * - 204 No Content si la suscripción fue cancelada.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const unsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDelete = <ThrowOnError extends boolean = false>(options: Options<UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteData, ThrowOnError>) => (options.client ?? client).delete<UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteResponses, UnsubscribeFromEventApiV1NotificationsSubscriptionsEventTypeDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/subscriptions/{event_type}',
    ...options
});

/**
 * Reload Custom Rules
 *
 * Recarga en caliente las reglas personalizadas (solo U3).
 *
 * Este endpoint permite a los administradores recargar las reglas personalizadas
 * sin necesidad de reiniciar el sistema, tal como exige el requisito MV6.2.1.
 *
 * Atributos:
 * - current_user: Usuario autenticado con rol U3.
 * - service: Servicio de reglas inyectado mediante dependencias.
 *
 * Retorna:
 * - 200 OK con el resultado de la recarga.
 * - 403 Forbidden si el usuario no es U3.
 * - 500 Internal Server Error para cualquier error inesperado.
 */
export const reloadCustomRulesApiV1AdminRulesReloadPost = <ThrowOnError extends boolean = false>(options?: Options<ReloadCustomRulesApiV1AdminRulesReloadPostData, ThrowOnError>) => (options?.client ?? client).post<ReloadCustomRulesApiV1AdminRulesReloadPostResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/rules/reload',
    ...options
});

/**
 * Health
 */
export const healthHealthGet = <ThrowOnError extends boolean = false>(options?: Options<HealthHealthGetData, ThrowOnError>) => (options?.client ?? client).get<HealthHealthGetResponses, unknown, ThrowOnError>({ url: '/health', ...options });
